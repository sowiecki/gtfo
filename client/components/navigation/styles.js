import { colors } from '../common/styles';

const { WHITE } = colors;

export const styles = {
  appBar: {
    position: 'absolute'
  },

  appTitle: {
    fontSize: '50px',
    fontWeight: '8',
    fontFamily: `'Wire One', sans-serif`
  },

  menuButton: {
    color: WHITE
  },

  navIcons: {
    marginLeft: '24'
  }
};
