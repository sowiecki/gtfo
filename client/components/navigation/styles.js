import { colors } from '../common/styles';

const { WHITE } = colors;

export const styles = {
  appBar: {
    // position: 'absolute',
    // backgroundColor: 'none',
    pointerEvents: 'none'
  },

  appTitle: {
    fontSize: '35px',
    fontWeight: '8',
    fontFamily: `'Wire One', sans-serif`
  },

  menuButton: {
    color: WHITE,
    pointerEvents: 'all'
  },

  navIcons: {
    marginLeft: '24'
  }
};
